# Base URL of the backend API.
# Local dev uses http://localhost:8000. For other environments, set this
# in .env.local (gitignored) or the host's environment settings.
VITE_API_BASE_URL=http://localhost:8000
