# Always revalidate the HTML shell so a new deploy is picked up on the
# next load. The hashed /assets/* files are safe to cache forever.
/
  Cache-Control: no-cache
/index.html
  Cache-Control: no-cache

/assets/*
  Cache-Control: public, max-age=31536000, immutable
